<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.neusou.vivacious"
      android:versionCode="1"
      android:versionName="0.01">
      
    <uses-permission android:name="android.permission.INTERNET" /> 
	<uses-sdk android:minSdkVersion="7" />
	
    <application android:icon="@drawable/icon" android:label="@string/app_name"
    android:name=".App"
    >	
    	<service android:name=".FlickrIntentService" android:enabled="true">
    	</service>
    	
    	<service android:name=".FlickrRestfulResponseProcessor" android:enabled="true">
    		<meta-data android:name="xtra_method" android:value="com.neusou.vivacious.flickr.restful.CALL_METHOD" />
    		<meta-data android:name="xtra_response" android:value="com.neusou.vivacious.flickr.restful.RESPONSE" />
    		<meta-data android:name="xtra_error" android:value="com.neusou.vivacious.flickr.restful.ERROR" />
    		<meta-data android:name="callback_intent" android:value="com.neusou.vivacious.flickr.restful.CALLBACK_INTENT" />
    		
    		<intent-filter>    		
    		 	<action android:name="com.neusou.vivacious.flickr.restful.PROCESS_RESPONSE"/>
    		</intent-filter>
    	</service>
    	
        <activity android:name=".Main"
                  android:label="@string/app_name"
                  android:configChanges="orientation"
                  >
                   <!-- This metadata entry causes .app.SearchQueryResults to be the default context -->
            <!-- whenever the user invokes search while in this Activity. -->
            <!-- 
            <meta-data android:name="android.app.default_searchable"
                       android:value=".Main" />
		 -->
		 
			<meta-data android:name="android.app.searchable"
                       android:resource="@xml/searchable" />
                        
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
              <!-- This intent-filter identifies this activity as "searchable" -->
            
             
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
             
        </activity>

    </application>
    
    

</manifest> 